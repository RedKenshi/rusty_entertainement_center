export struct TreeItem {
    name: string,
    format: string,
    codec: string,
    bitrate: string,
    resolution: string,
    size: string,
    indent: int,
    is_folder: bool,
    is_volume: bool,
    expanded: bool,
    selected: bool,
    path: string,
    nbFiles: int,
    filesSize: string,
    filesDuration: string,
}
