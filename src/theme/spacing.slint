export global Spacing {
  // Teal
  in property <length> xs: 1px;
  in property <length> s: 4px;
  in property <length> m: 8px;
  in property <length> l: 16px;
}

export global BorderRadius {
  // Teal
  in property <length> s: 2px;
  in property <length> m: 4px;
  in property <length> l: 8px;
}
